apiVersion: v1
kind: Service
metadata:
  name: iot-gateway-service
  namespace: fms-iot-gateway
  labels:
    app: iot-gateway-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: iot-gateway-service
